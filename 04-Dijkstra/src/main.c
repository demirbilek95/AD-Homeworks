#include <stdio.h>
#include "dijkstra.h"

int main(){

    // Checking the correctness of algorithm
    printf("Bakalım compile edilecebileck mi?");
    unsigned int size = 6;
    unsigned int ** adj_matrix = (unsigned int *)malloc(sizeof(unsigned int)*size*size);

}